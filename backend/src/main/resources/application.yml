spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/tcm_ai?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

wx:
  appid: wx55616a24e80f91b2  # 你的小程序appid
  secret: eed481364eea086d1b3f0da865e5bffa  # 你的小程序secret
  url: https://api.weixin.qq.com/sns/jscode2session
jwt:
  secret: your-256bit-secret-key-which-should-be-at-least-32-characters
  expiration: 604800000  # 7天（毫秒）
  token-name: authentication  # 指定 Token 在请求头中的键名（如前端传递时用这个键）
server:
  port: 8080